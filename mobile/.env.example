# Referência — a URL ativa fica em capacitor.config.json → server.url
# Produção (atual): https://bwipo.com/ (HTTP redireciona para HTTPS)
# Dev: https://crm-dev-frontend.ca31ey.easypanel.host
# Host legado (não usar): https://frontend-front.v74knz.easypanel.host
# Mudar server.url exige novo APK + `npm run sync` (não basta deploy web).
CRM_MOBILE_SERVER_URL=https://bwipo.com/login
