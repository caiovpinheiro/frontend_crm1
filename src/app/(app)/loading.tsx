import { RouteLoading } from "@/components/crm/page-loading";

/** Navegação entre rotas do app: marca 32px no main, NavRail já no layout. */
export default function AppSegmentLoading() {
  return <RouteLoading />;
}
