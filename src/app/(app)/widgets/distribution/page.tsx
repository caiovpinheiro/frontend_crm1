/**
 * /widgets/distribution — Inteligente ou Por Leads (consultores).
 * O chrome vive no layout; esta página só ancora a rota.
 */

export const dynamic = "force-dynamic";

export default function DistributionPage() {
  return null;
}
